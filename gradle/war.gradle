apply plugin: "war"

bootWar {
    mainClassName = "org.unibremen.mcyl.androidslicer.AndroidSlicerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    webAppDirName = "build/resources/main/static/"
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
